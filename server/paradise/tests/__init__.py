"""Paradise test suite — lives on `prod` branch.

These tests only run when prod-only deps are installed
(`pip install -r server/requirements-prod.txt`).
Main branch's existing tests (none currently) live elsewhere.
"""
