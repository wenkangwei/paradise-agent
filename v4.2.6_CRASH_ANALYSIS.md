# v4.2.6 启动闪退分析与修复方案

> 当前状态：已 git revert 98db5a7（commit `0cbd36d`），代码回到 v4.2.5
> 已重编 APK：`/mnt/c/Users/wenka/Desktop/aichat-debug-v4.2.5-rollback.apk`
> **本文件只分析方案，不动代码。审核通过后再实施。**

---

## 一、回退已完成

```
0cbd36d Revert "fix: [v4.2.6] ..."   ← HEAD（= v4.2.5 代码状态）
98db5a7 fix: [v4.2.6] ...            ← 保留历史，未删除
d789b19 fix: [v4.2.5] ...
```

代码层面 = v4.2.5。APK 已重编 → `/mnt/c/Users/wenka/Desktop/aichat-debug-v4.2.5-rollback.apk`。

### ⚠️ 用户必须做这一步（否则装上 v4.2.5 rollback 还会闪退）

v4.2.6 之前已经把设备上的 DB schema 升到 **v10**（messages 表多了 `updatedAt` 列 + 索引）。
v4.2.5 代码里的 `@Database(version = 9)` 期望 v9 schema，Room 打开 v10 DB 时找不到 10→9 的 downgrade migration → **继续闪退**。

**操作**：
1. 手机上卸载当前的 AiChat（v4.2.6）
2. 装新 APK：`aichat-debug-v4.2.5-rollback.apk`
3. 卸载会清掉所有本地数据（对话历史 / API key / 收藏）。如果用户有重要数据想保留，需要先 adb backup（复杂），否则就接受清空。

---

## 二、v4.2.6 为什么闪退（最可能的根因）

v4.2.6 改了 7 个文件，启动时最可能炸的有两个嫌疑点：

### 嫌疑 1（最大）：Room DAO 加了 Kotlin 默认参数

v4.2.6 的 `MessageDao.kt`：
```kotlin
@Query("UPDATE messages SET content = :content, updatedAt = :updatedAt WHERE id = :id")
suspend fun updateContent(
    id: String,
    content: String,
    updatedAt: Long = System.currentTimeMillis()   // ← 默认参数
)

@Query("UPDATE messages SET status = :status, updatedAt = :updatedAt WHERE id = :id")
suspend fun updateStatus(
    id: String,
    status: String,
    updatedAt: Long = System.currentTimeMillis()   // ← 默认参数
)
```

**问题**：Room 2.6.1 对 `suspend fun` + Kotlin 默认参数组合存在已知问题。
- Room 编译期生成 `MessageDao_Impl`（Java），只实现**完整签名**（三参数）的 suspend 方法。
- Kotlin 调用方 `messageDao.updateContent(id, content)`（两参数）经编译器合成调用 `DefaultImpls.updateContent$default(...)`，但 suspend 的 Continuation 传递路径与默认参数 bitmask 在某些 Kotlin / Room 版本组合下生成的字节码会找不到匹配方法。
- 启动时 Hilt 注入 ChatViewModel → ChatRepositoryImpl → MessageDao_Impl。Room 第一次实例化 DAO 不会立刻调用方法，**但 Application 第一次开 DB 时 Room 会验证所有 DAO 类可加载**。如果 Impl 类有方法签名错配，ClassNotFoundException 或 NoSuchMethodError 直接抛。

**间接证据**：编译期 KSP 不报错（Kotlin 默认参数在 Kotlin 看来合法），运行期才暴露。这正是"build SUCCESSFUL 但启动闪退"的典型 pattern。

### 嫌疑 2（次要）：MessageEntity 字段默认值引用其他字段

v4.2.6 的 `MessageEntity.kt`：
```kotlin
data class MessageEntity(
    /* ... */
    val timestamp: Long,
    /* ... */
    val reaction: String? = null,
    val updatedAt: Long = timestamp    // ← 引用上面的 timestamp 作为默认值
)
```

Room 2.6.x 支持这种写法，但早期版本会报 `Cannot figure out how to save this field into database`。如果 Room codegen 没正确处理 `= timestamp` 这种 cross-field default，生成的 EntityCodec 会编译失败 —— 但 build 过了，所以这个嫌疑较弱。

### 排除项（不会引起启动闪退）
- MIGRATION_9_10 的 SQL（ALTER / UPDATE / CREATE INDEX）逻辑无问题，标准 SQL。
- ChatViewModel.init 里 `markDanglingStreamingInterrupted(reason, olderThan)` 三参数签名，DAO / Repository / ViewModel 三处对齐。
- Hilt module 没改动。

### 还需要的信息（强烈建议用户提供）
- **logcat 全栈**：`adb logcat -v threadtime | grep -i "aichat\|androidruntime\|fatal"` 抓启动到闪退的完整 stacktrace。这才是 100% 确认根因的依据。如果不方便抓，可以只看 AndroidManifest 里 `ActivityManager: FATAL EXCEPTION` 那一段的 className + methodName + line。

---

## 三、修复方案（等用户审核）

### 方案 A：完全去掉 Kotlin 默认参数（推荐）

把 v4.2.6 的 DAO / Repository / ViewModel 改成**显式传 updatedAt**，避免任何默认参数：

```kotlin
// MessageDao.kt — 改为
@Query("UPDATE messages SET content = :content, updatedAt = :updatedAt WHERE id = :id")
suspend fun updateContent(id: String, content: String, updatedAt: Long)

@Query("UPDATE messages SET status = :status, updatedAt = :updatedAt WHERE id = :id")
suspend fun updateStatus(id: String, status: String, updatedAt: Long)

@Query("UPDATE messages SET reasoningContent = :content, updatedAt = :updatedAt WHERE id = :id")
suspend fun updateReasoning(id: String, content: String, updatedAt: Long)
```

```kotlin
// ChatRepositoryImpl.kt — 改为
override suspend fun updateStreamingMessage(...) = withContext(ioDispatcher) {
    val now = System.currentTimeMillis()
    messageDao.updateContent(id, content, now)
    if (reasoningContent != null) {
        messageDao.updateReasoning(id, reasoningContent, now)
    }
    messageDao.updateStatus(id, status, now)
}

override suspend fun updateMessage(id: String, content: String) = withContext(ioDispatcher) {
    messageDao.updateContent(id, content, System.currentTimeMillis())
}
```

其他部分（entity 字段 / migration / watchdog 阈值）保持 v4.2.6 原样。
**收益**：消除嫌疑 1，保留 v4.2.6 的核心修复（updatedAt 列 + stale-only watchdog）。

### 方案 B：放弃 updatedAt 字段，换另一种"活跃流"识别策略

如果方案 A 改完仍然闪退（说明嫌疑 1 不是真因），就完全不动 schema：

- 不加 `updatedAt` 列
- watchdog 改成"只动 5 分钟前插入的 STREAMING 行"（用现有 `timestamp` 字段过滤）
- 缺点：`timestamp` 是插入时间不是最后更新时间，长时间流式（>5min）会被误判，但实际场景里 LLM 流很少超 5 分钟，可接受

### 方案 C：保留 v4.2.6 代码不动，等用户提供 logcat 后精准修复

不主动改代码，让用户抓一次 logcat 给我，根据真实 stacktrace 修。**最稳但最慢**。

---

## 四、推荐路径

1. **现在**：用户先卸载 v4.2.6 + 装 `aichat-debug-v4.2.5-rollback.apk`，确认 v4.2.5 能正常打开（验证不是别的问题）
2. **可选但强烈建议**：抓一次 v4.2.6 的启动 logcat（先装回 v4.2.6 APK 抓崩溃栈，再卸载装 rollback）
3. **审核本方案**：用户选 A / B / C
4. 选定后我再动手改 + 编译 + 拷 APK

---

## 五、待办（审核通过后执行）

- [ ] 用户审核方案 A/B/C
- [ ] （可选）拿 v4.2.6 logcat 验证嫌疑 1
- [ ] 实施选定方案
- [ ] 编译 + cp APK
- [ ] 提交（不 amend，新 commit；这次保留 revert 历史，不抹掉痕迹）
